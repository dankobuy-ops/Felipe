service-account.json
*.json.key
.env

# Google auth + provisioned config (never commit) — PJUD + JPL share the account
client_secret*.json
token.json
pjud_config.json
jpl_config.json
application_default_credentials.json
__pycache__/
*.pyc

# Dev artifacts — screenshots & logs (any project), kept local, never committed
Screenshots/
screenshots/
Logs/
logs/
*.log

# PJUD network-probe dumps — local experiment artifacts (may hold request internals)
netprobe_*.jsonl
pjud_cdp_*.json

# PJUD worker output — scraped causa data (names, RUTs) and downloaded PDFs.
# This belongs in Neon and Drive, never in git.
pjud/data/

# Local Claude Code config
.claude/
